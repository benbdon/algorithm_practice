{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build insertion_sort",
            "type": "shell",
            "linux": {
                "command": "g++ -g insertion_sort.cpp -o main.o",
            },
            "windows": {
                "command": "g++ -g insertion_sort.cpp -o main.exe"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}